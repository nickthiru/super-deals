/**
 * Auth Types
 * 
 * This module exports all authentication-related type definitions.
 * 
 * @module @super-deals/types/auth
 */

// Re-export all auth-related types
// As we add more type definitions, we'll export them here

/**
 * @typedef {import('./verification').VerificationResponse} VerificationResponse
 * @typedef {import('./verification').ResendVerificationResponse} ResendVerificationResponse
 * @typedef {import('./verification').VerificationFormData} VerificationFormData
 */

// Export empty object since JSDoc types are used for documentation only
module.exports = {};
