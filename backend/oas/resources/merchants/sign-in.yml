post:
  tags:
    - sign-in
  description: ???
  security:
    - basic: []
  requestBody:
    required: true
    description: ???
    content:
      application/json:
        schema:
          $ref: "../../schemas/SignInForm.yml"
  responses:
    "200":
      description: ???
      content:
        application/json:
          schema:
            type: object
            required:
              - message
            properties:
              message:
                type: string
                example: "Account registered. Complete sign-up with OTP"
