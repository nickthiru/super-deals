/** @type {import('./$types').LayoutLoad} */
export function load() {
    return {
        sections: [
            { slug: 'sign-in', title: 'Sign In' },
            { slug: 'sign-up', title: 'Sign Up' }
        ]
    };
}
